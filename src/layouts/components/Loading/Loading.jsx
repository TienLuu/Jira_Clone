import Spinner from "../../../components/Spinner/Spinner";
import { Wrapper } from "./Styles";

const Loading = () => {
   return (
      <Wrapper>
         <Spinner></Spinner>
      </Wrapper>
   );
};

export default Loading;
